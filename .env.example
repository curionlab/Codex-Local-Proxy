# ---- LM Studio / Local LLM Settings ----
# The URL of your local LLM server (e.g., LM Studio uses 1234 by default)
UPSTREAM_LLM_BASE_URL=http://127.0.0.1:1234/v1
UPSTREAM_LLM_KEY=lm-studio

# Proxy Server Port (Matches the base_url port in Codex config)
PORT=3001
